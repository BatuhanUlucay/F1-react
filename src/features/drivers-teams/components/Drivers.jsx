import React from 'react'
import { useDriverRankings } from '../../rankings/api/getDriverRankings'

function Drivers() {

  // const currentDriversQuery = useDriverRankings(new Date().getFullYear());

  



  //TODO: fetch current driver ids, 
    // then show cards of drivers. routing to the detail pages.


  return (
    <div>Drivers</div>
  )
}

export default Drivers